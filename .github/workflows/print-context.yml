on: push

jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github.event.repository.default_branch) }}
        run: echo "$GITHUB_CONTEXT"
